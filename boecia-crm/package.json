{
  "name": "boecia-crm",
  "private": true,
  "type": "module",
  "description": "Boecia Talent CRM: static site that edits the CRM Google Sheet",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "serve": "python3 -m http.server 8080 --directory public"
  }
}
